/*
var AM = new AssetManager();


function EventScenario(game, dungeon){
    this.dungeon = dungeon;
    this.opacity = 1;
    this.game = game;
    this.ctx = game.ctx;
    this.rest_option = new Rest_Option(game, dungeon, dungeon.PlayerCharacter, AM.getAsset("./img/travel/rest_option.png"), this, 1)
    this.smith_option = new Smith_Option(game, dungeon, dungeon.PlayerCharacter, AM.getAsset("./img/travel/smith_option.png"), this, 1)
    this.display_options = true;
};



CampfireScene.prototype.update = function () {     
    if (this.display_options) {
        this.rest_option.update();
        this.smith_option.update();
    }
};

CampfireScene.prototype.draw= function() {
    if (this.display_options) {
        this.rest_option.draw();
        this.smith_option.draw();
    }
}
*/